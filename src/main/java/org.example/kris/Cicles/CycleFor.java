package org.example.kris.Cicles;

import java.util.Scanner;

public class CycleFor {
    public static void main(String[] args) {
        Scanner pensiAge = new Scanner (System.in);
        int age = pensiAge.nextInt();
        boolean isNePensia = (20 <= age) || (age <= 60);
        boolean isPensia = (20 >= age) || (age >= 60);
        if (isPensia ){

            }
        }
}






